{
  "name": "autoverifica",
  "version": "3.0.0",
  "private": true,
  "description": "PWA pre-acquisto che consulta lo storico revisioni ufficiale del Portale dell'Automobilista con CAPTCHA risolto dall'utente.",
  "main": "server.js",
  "scripts": {
    "postinstall": "npx puppeteer browsers install chrome",
    "start": "node verify-browser.js && node server.js"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer": "^24.16.0"
  }
}
